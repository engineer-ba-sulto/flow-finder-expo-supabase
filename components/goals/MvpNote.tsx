import React from "react";
import { Text, View } from "react-native";

export const MvpNote: React.FC = () => (
  <View className="mt-6 p-3 bg-blue-50 rounded-xl">
    <Text className="text-xs text-blue-600">
      💡 MVP1: 基本CRUD機能のみ実装済み
    </Text>
  </View>
);
